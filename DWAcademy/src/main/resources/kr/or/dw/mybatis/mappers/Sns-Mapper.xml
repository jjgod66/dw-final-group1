<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper 
	PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" 
	"http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="kr.or.dw.dao.SnsDAO">
	<select id="selectByMemberCode">
		SELECT mem_cd
		  FROM mem_sns_login
		 WHERE email = #{email}
	</select>
	
	<insert id="insert">
		INSERT INTO mem_sns_login (mem_cd, sns, sns_name, sns_email, linkdate, access_token, refresh_token)
							VALUES (#{mem_cd}, 'K', #{sns_name}, #{sns_email}, SYSDATE, #{access_Token}, #{refresh_Token})
	</insert>
	
	<select id="selectSnsInfo" resultType="sns">
		SELECT *
		  FROM mem_sns_login
		 WHERE mem_cd = #{mem_cd}
	</select>
</mapper>