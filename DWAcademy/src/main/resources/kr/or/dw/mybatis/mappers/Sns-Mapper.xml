<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper 
	PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" 
	"http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="kr.or.dw.dao.SnsDAO">
	<select id="selectByMemberCode" resultType="sns">
		SELECT mem_cd
		  FROM mem_sns_login
		 WHERE sns_email = #{sns_email}
	</select>
	
	<insert id="insert">
		INSERT INTO mem_sns_login (mem_cd, sns, linkdate, access_token, refresh_token, sns_email)
							VALUES (#{mem_cd}, 'K', SYSDATE, #{access_Token}, #{refresh_Token}, #{sns_email})
	</insert>
	
	<select id="selectSnsInfo" resultType="sns">
		SELECT *
		  FROM mem_sns_login
		 WHERE mem_cd = #{mem_cd}
	</select>
</mapper>